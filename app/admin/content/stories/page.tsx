import { redirect } from 'next/navigation'

// app/admin/content/stories/page.tsx
//
// Перенаправлення на єдиний каталог творів.
//
// 15.09.2026 список звідси зведено з /admin/works: обидві сторінки показували
// той самий набір записів, різниця була лише в тому, чи видно чернетки.
// Сторінка лишається файлом, а не видаляється, з двох причин: на цю адресу
// веде перемикач «Балабони» в шапці адмінки, і вона могла осісти в закладках.
//
// ВАЖЛИВО: підсторінка /admin/content/stories/[id]/edit жива й активно
// використовується — її відкриває кнопка «Відкрити» в каталозі та посилання
// зі списку серій. Перенаправлення стосується тільки самого списку.

export default function ContentStoriesRedirect() {
  redirect('/admin/works')
}
